<h1> All articles </h1>

<%= link_to 'Add a new article', new_article_path(@article) %>

<br>
<br>

  <% @articles.each do |article| %>
    <h4><%= link_to article.title, article_path(article) %></h2>
    <p><%= article.content %></p>
  <% end %>

</ul>
